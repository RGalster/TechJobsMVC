<div class="container body-content">

    <h1>@ViewBag.title</h1>

    <form action="/search/results" method="post">
        <h2>Search by:</h2>

        <p>
            @foreach (var column in ViewBag.columns)
            {
                <span>
                    <input type="radio" name="searchType" id="@column.Key" value="@column.Key" checked="@column.Key == 'all'" />
                    <label for="@column.Key">@column.Value</label>
                </span>
            }

        </p>

        <p>
            <label for="searchTerm">Keyword:</label>
            <input type="text" name="searchTerm" id="searchTerm" />
        </p>

        <input type="submit" value="Search" />
    </form>

    <hr />

    @{
        //TODO #4: Loop over the search results to display all job fields.
    }

@*make a new div here?*@

@*the code copied from my list view*@
<table class="centeredTable">
    <tr>
        @foreach(var column in ViewBag.columns)
        {
            <th>@column.Value</th>
        }
    </tr>
    <tr>
        @{
            //no longer todo 2/ that task was completed by this point:
            //TODO #2: Complete the View Jobs By Category Table
            //i think i can just delete all of this
        }

        <td>
            <ul>
                <li><a asp-controller="List" asp-action="Jobs" asp-route-column="All" asp-route-value="all">All Jobs</a>
                    </li>
                    </ul>
                    </td> 
                    @* Feel free to remove or modify this element if necessary. *@
                    @*exception thrown on line below/ still working on the Search Controller though*@
        @foreach (var category in ViewBag.tableChoices)
        {
            <td>
                <ul>
                    @foreach (var item in category.Value)
                    {
                        <li>
                            <a asp-controller="List" asp-action="Jobs" asp-route-column="@category.Key" asp-route-value="@item">@item</a>
                        </li>
                    }
                </ul>
            </td>
        }
    </tr>
        </table>

</div>